<% provide(:title, 'Categories') %>
<h1>Categories</h1>

<% if logged_in? %>
	<%= link_to "Create new Category", new_category_path, class: "btn btn-primary" %>
<% end %>

<%= will_paginate %>

<ul class="categories">
  <%= render @categories %>
</ul>

<%= will_paginate %>