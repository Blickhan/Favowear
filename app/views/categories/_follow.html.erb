<%= form_for(current_user.followings.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :category_id, @category.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary btn-xs" %>
<% end %>